enum ButtonIconType {
  SEARCH = 'search',
  PLUS = 'plus',
  MINUS = 'minus',
  EDIT = 'edit',
  DELETE = 'delete',
  COMPLETE = 'complete',
  REDIRECT = 'redirect',
}

enum ButtonColor {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  DANGER = 'danger',
  SUCCEED = 'succeed',
}

export { ButtonIconType, ButtonColor };
